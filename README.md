# NC News Seeding

- you will need to set up two env files
  -this will hold the information for selecting the default database name
  -you will need to use the syntax PGDATABASE='ADD_DATABASE_NAME_HERE'
